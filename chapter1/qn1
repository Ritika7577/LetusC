/*Ramesh’s basic salary is input through the keyboard. His dearness allowance is 40% of basic 
salary, and house rent allowance is 20% of basic salary. Write a program to calculate his gross salary*/
#include <stdio.h>
int main(){
    int basic_sal;
    float dearness_allowance, house_rent, gross_sal;
    printf("Enter basic salary :");
    scanf("%d", &basic_sal);
    dearness_allowance = basic_sal * 0.4;
    house_rent = basic_sal * 0.2;
    gross_sal = basic_sal + dearness_allowance + house_rent;
    printf("Gross salary %.2f", gross_sal);
    return 0;
}
